export const players = (p) => ({
    type: 'PLAYERS',
    players: p
})

export const search = (s) => ({
    type: 'SEARCH',
    search: s
})

export const ordering = (o) => ({
    type: 'ORDERING',
    ordering: o
})

export const page = (p) => ({
    type: 'PAGE',
    page: p
})